# Copie para deploy/.env e preencha. NÃO versione o .env preenchido.

# URL pública HTTPS da própria Evolution nesta VPS (atrás de um proxy/SSL).
# Domínio que o Caddy vai publicar com TLS. O DNS precisa apontar para o IP
# desta VPS ANTES de subir a stack, senão a emissão do certificado falha.
# Ex.: evo.cardiocentrojf.com.br
EVOLUTION_DOMINIO=

SERVER_URL=https://evo.suaclinica.com.br

# Chave da Evolution — DEVE ser a MESMA que o app usa em EVOLUTION_API_KEY.
EVOLUTION_API_KEY=troque-por-uma-chave-forte

# Senha do Postgres da stack (só interna à VPS).
POSTGRES_PASSWORD=troque-esta-senha

# Webhook do app na Vercel que recebe as mensagens.
WEBHOOK_URL=https://SEU-APP.vercel.app/api/whatsapp/evolution/webhook

# Segredo que a Evolution manda no header x-evolution-secret de cada webhook.
# DEVE ser o MESMO valor de EVOLUTION_WEBHOOK_SECRET na Vercel — sem ele, em
# produção o app rejeita todas as mensagens (fail-closed).
EVOLUTION_WEBHOOK_SECRET=

# Nome da instância no Evolution. DEVE ser o MESMO de EVOLUTION_INSTANCE na
# Vercel. Usado por ./deploy.sh qr para criar a instância e gerar o pareamento.
EVOLUTION_INSTANCE=cardiocentro
